<%= form_with(model: note, local: true) do |form| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :note_title %>
    <%= form.text_field :note_title, id: :note_note_title %>
  </div>

  <div class="field">
    <%= form.label :note_contents %>
    <%= form.text_area :note_contents, id: :note_note_contents %>
  </div>

  <div class="field">
    <%= form.label :note_auth %>
    <%= form.text_field :note_auth, id: :note_note_auth %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
